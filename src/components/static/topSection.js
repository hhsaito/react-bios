import React from 'react';

const topSection = () => {
    return (
        <section  className="section-darker section-downward">
            <div className="container">
                <h2>React Example</h2>
                <p>This is a React.js version of the accessible modal that I'm in the process of building. Currently, the elements have been moved into components and the search has been moved into React.</p>
                <p>The code for this can be found on <a href="https://github.com/hhsaito/react-bios">Github</a>.</p>
            </div>
        </section>
    );
}

export default topSection;